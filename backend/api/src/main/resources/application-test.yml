spring:
  config:
    activate:
      on-profile: test
jwt:
  secret-key: secretsecretsecretsecretsecretsecretsecretsecretsecretsecret
  access-token-expires-days: 1

oauth2:
  google:
    client-id: client-id
    client-secret: client-secret
    redirect-uri: redirect-uri

selenium:
  chrome-options:
    - "--headless=new" # 크롬창 노출 x
    - "--window-size=1920,1080"