---
spring:
  config:
    activate:
      on-profile: local
selenium:
  chrome-options:
    - "--no-sandbox"

---
spring:
  config:
    activate:
      on-profile: prod
selenium:
  chrome-options:
    - "--headless" # 크롬창 노출 x
    - "--no-sandbox"