spring:
  jpa:
    database: mysql
#    database-platform: org.hibernate.dialect.MySQLDialect
    open-in-view: false

---
spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:mysql://localhost:3306/blogzip
    username: blogzip
    password: blogzip
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always

---
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: jdbc:mysql://localhost:3306/blogzip # todo 수정
    username: blogzip
    password: blogzip
  sql:
    init:
      mode: never